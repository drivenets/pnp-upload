import { IHttpClientImpl, IFetchOptions } from '@pnp/common-commonjs';
import { IAuthOptions } from 'node-sp-auth';
import { AuthConfig } from 'node-sp-auth-config';
export default class NodeFetchClient implements IHttpClientImpl {
    private authData;
    private siteUrl?;
    private authSettings;
    constructor(authData: IAuthOptions | AuthConfig | string, siteUrl?: string);
    fetch(url: string, options: IFetchOptions): Promise<any>;
    private initAuthOptions;
    private initAuthOptionsFromAuthConfig;
    private mergeHeaders;
}
